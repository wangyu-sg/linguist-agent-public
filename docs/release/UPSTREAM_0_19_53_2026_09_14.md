# Proma v0.19.53 本地合并候选

核验日期：2026-09-14。当前为本地候选，没有推送、Release 或日用安装。

## 来源与实现

- 工作副本：`.worktrees/la-upstream-v0.19.53-20260914`；分支 `codex/la-upstream-v0.19.53-20260914`。
- LA 起点：`9f0de928697067e9e41841d7de8d04c5de317bd6`。
- 上游：[`v0.19.53`](https://github.com/proma-ai/Proma/releases/tag/v0.19.53)，固定 `f99edbdb594407ab190b97ae073889c5d96637ab`。
- 正式双亲合并：`56bc3f29b225c71f02f42483e10f1db8c3fcb2d5`；父提交分别为以上 LA 起点与上游固定提交。起点未含 .52，本轮一次合入累计变化。
- App 保持 `0.17.73`，Pi 五项 overrides 与四项 App 依赖更新到 `0.85.1`；CAT Core/Formats/Store/Tools、Schema 19、32 个 CAT 工具和四岗位 Prompt 均未改动。
- 上游 Exa 卡片、DeepSeek Flash/profile/1M 上下文、MCP 管理与 OAuth、inactive Skill 管理、Copilot 额度、Markdown 多行表格及文件图片路径能力已合入。
- 侧栏保留 LA 共用会话树、项目绑定和键盘入口，接入上游层级 token 与滚动边界。无委派时的数量/展开占位在起点已经不存在；本轮不声称再次修复该问题。
- 保留 LA 文件 chip 的单一解析状态，补入消息 owner 和缓存会话隔离；未恢复已删除的重复 utils。Exa 凭据存储沿用已有 entry URL，不新增重复地址配置。
- README 与 AGENTS 保持原样。原始 checkout 未改动，红绿灯按钮继续延期。

## 已取得证据

| 命令/检查 | 结果与范围 |
|---|---|
| `bun install --frozen-lockfile` | 通过，锁文件无变化 |
| `bun run typecheck` | 全部 workspace 通过 |
| `bun run test` | 完整既有回归通过，包含 Store、Tools、Stage/交付证据与 host lifecycle |
| 定向集成与升级回归 | Exa 3 项、DeepSeek/MCP/表格 3 项通过；已纳入根 test |
| `bun run electron:build` | 通过，固定 OfficeCLI 校验及原生资源构建完成 |
| `bun run check:boundaries` | 通过；新基线下 ledger 277 项，无 stale/unregistered 路径 |
| `bun run license:scan` | 通过；实际生产闭包 519 个，完整快照已同步 |
| Pi 安装内容与 localhost 请求 | 使用实际 SDK 与合成 HTTP 200 SSE；GPT-5.6 Sol long 为 `prompt_cache_options: {ttl: "30m"}`，不含旧 retention；short 不强加字段；GPT-5.5 long 仍为 `24h`。补丁瞬时错误分类与 aborted 不重试共 5 项通过 |

Pi 官方变更来源：[v0.85.1 Changelog](https://github.com/earendil-works/pi/blob/v0.85.1/packages/coding-agent/CHANGELOG.md)。实际模型为目录中的 `gpt-5.6-sol`，不是不存在的裸 `gpt-5.6` catalog 项。此检查证明 SDK 适用模型与配置下的请求形状，不代表真实服务、缓存命中或产品默认启用长缓存。

旧 `deepseek-v4-flash` 是上游保留的独立模型；本次新候选 `deepseek-flash-v1` 只向缺少新 ID 的现有渠道追加 disabled 项，并以更新标记防止重复。没有批量改写用户端点、凭据、选中模型或历史；未知模型继续使用原有默认上下文。

## 打包验证

最终 `bun run smoke:vertical` 六步均 passed / exitCode=0，日志非空；源码 `e8ff739a8a058773e25698a6c0c453d294d1eab2`，工作树干净。后续提交只补本记录与当前事实，不改变打包代码。

- 平台：macOS arm64，Electron `43.2.0`，App `0.17.73`，Pi `0.85.1`。
- `app.asar` SHA-256：`dc6d4c6b5594f8905a487e01e565868dc1d020c4b434e3b5aab11d69a98d2ddf`；已重新计算并与报告比对。
- 包内 Pi Coding Agent / Agent Core / AI / Server 版本和入口检查通过；retry 实现与已验证安装内容逐字一致。旧、新 retry patch 的 Git blob 均为 `37c60b1c5eedd2278f323502ae3b00d12e68b613`。
- Agent 19 PASS / 0 FAIL：真实 utility → HTTP → 主进程 Receipt、Streaming、Thinking、Stop、同会话 Retry、CAT 不可用时通用对话继续。
- Chat 19 PASS / 0 FAIL：流式、工具续接、429 重试、停止、重启恢复。
- 项目快速切换通过；Linguist 31 PASS / 0 FAIL / 2 MANUAL：批次建议、QA、阶段确认、备份/恢复、worker、导出重导和持久化。
- 辅助面板专项 26 PASS / 0 FAIL；深浅主题、900px/800px 编辑、草稿、Dock 恢复与只读预览检查通过。
- 证据在工作副本 `apps/electron/out/smoke/vertical/vertical-smoke-report.json` 及其六步日志；任务诊断目录 `/tmp/la-01953-evidence` 保留请求、测试、首次失败和复验记录。

首次六步尝试在窄窗探针失败：设置横滚后只等待一帧，记录位置为 240px，QA 列未进入视口；随后恢复已离屏的 TM Tab 时点击超时。同一产物的完整重放和十次缩放检查未复现为持续产品缺陷，横滚末端均为 413px。因此没有改动 CAT 布局；`e8ff739a` 让探针在有界帧数内确认达到当前滚动末端后再断言，并用键盘恢复 Tab。原始 QA 可见性、编辑命中与草稿断言保留；最终从该干净提交重新执行完整六步并通过。

## 补充控制流与差异

隔离 Provider 配置回归共 15 项通过（既有 14 项加新候选检查），证实 DeepSeek 存量追加 disabled、新旧模型与端点/凭据保留、重复列举不重复写入。MCP 模拟握手检查通过：失败关闭、成功下一轮可用、未经允许的连接替换零写入。这些是本地模拟控制流，不是远程服务认证。

`app-shell / agent / chat / session-tree` 同一目录集合的 numstat：

| 比较 | 文件数 | 增加/删除行 |
|---|---:|---:|
| 旧基线 → LA 起点 | 48 | +3233 / -1500 |
| 旧基线 → 本次候选 | 50 | +3349 / -1645 |
| 新基线 → 本次候选 | 48 | +3203 / -1450 |

前两行使用相同旧基线，第三行只用于说明升级后剩余差异，不能把更换比较基线造成的数字变化宣称为删除了自定义 UI。剩余差异主要是三模式、项目绑定、原生右侧 CAT 扩展、共用会话树和既有安全/恢复接缝；详细登记见 [触点](../architecture/proma-touchpoints.json)。

冻结安装、license check、文档合同 12 项、boundary 4 项、fusion 14 项、Host Seam 12 处与同步演练均通过。生产源码与脚本没有引用已撤回的 `pi-coding-agent/client`、`experimental/plugin` 或 `pi-client`。

## 证据边界

真实 Exa/API Key/OAuth/Keychain、收费 Provider、语言质量、IME、VoiceOver 和 Native Open/Save 未验证。发布资格和 14 天日用仍 pending；Fake Provider 与自动化不能替代这些证据。详细未完成项见 [TODO](../../TODO.md)。
