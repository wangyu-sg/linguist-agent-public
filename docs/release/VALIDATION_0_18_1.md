# 0.18.1 实施与发布验证

更新日期：2026-09-22。

## 状态

`v0.18.1` 已于 `2026-09-22T14:35:44Z`（北京时间 22:35:44）[公开发布](https://github.com/wangyu-sg/linguist-agent-public/releases/tag/v0.18.1)，为 latest，非草稿、非预发布。完整本地测试、类型、边界、宿主接缝以及发布源码对应的远程六步检查通过，Auto Release 成功，七项资产已上传。实际下载哈希、两个 macOS 包签名及更新清单核验通过。用户随后明确授权替换日用安装，正式 arm64 包安装、启动、启动后验签及手动检查更新通过。本文不授予完整产品资格。

| 项目 | 发布事实 |
|---|---|
| App | `0.18.1` |
| 公开 Proma 源码基线 | `v0.19.57` / `4e96c5e859302c4a34618d45db352b29a7ebeb28` |
| Pi Runtime | `0.86.1` |
| Shared | `0.1.73` |
| CAT Schema / CAT 工具数 | `19` / `32`；另有会话级文件工作副本工具 |
| 正式上游合并 / 候选 commit | `b9417191d80d96e15a2702c9f8f927327312a970` / `397a474f` |
| 发布源码 / Tag commit | `c85c7d8e206737adf92340001290f0bdf0a70620` |
| 公开 Tag / CI / Release run | `v0.18.1` / `35739485354` attempt 1 成功 / `35740252627` 成功 |
| 安装包 / 更新元数据 | 七项资产齐全；三个更新资产哈希、两个 macOS 包签名及清单关联已核验 |
| 日用安装 / 在线更新 | 已授权替换为正式 arm64 包；检查更新通过，下一版本自动下载/安装全链未验证 |

## 本次变化

- Linguist 复用通用 Agent 的原生顶栏、消息、输入框和右侧工作区，CAT 作为工作区内容接入；Chat 保留原生 Chat 路径。三模式共享外壳与导航保持上游行为，领域岗位、项目身份、授权及结构保护保留。
- 恢复上游审批区、会话拖入引用、队列与消息排版、文件/Markdown/浏览器菜单行为；移除额外的窄窗强制折叠和右区隐藏。
- 统一导航代际与最近访问记录，修复项目会话恢复、冷启动收起状态、设置入口、改名显示及同名预览身份。
- 后台终端不再依赖右栏挂载来收集事件；隐藏的子任务不清查看提醒，展开后再清。两项均为在上游现有路径上确认的修复。
- 继续新任务与重试保留正确的项目、岗位和原轮次范围；解绑后同步模式。
- Pi 0.86.1 保留原生生命周期和请求路径；本轮发现的项目指令通过原生轮末队列交付，避免规则发现后工具在同一任务中持续被阻断。

## 基线与安装包对照边界

公开 `v0.19.57` 与 `v0.19.62` Tag 指向同一 commit；该公开源码的 App 版本仍为 0.19.57。另行只读检查的 0.19.62 安装包具有不同版本信息与额外同步进度接口。对照已覆盖 Agent/Chat 顶栏、主区显示条件、中心标签栏、右区菜单及主要布局路径；不能据此证明整个安装包与公开源码相同，不能宣称完整合入 0.19.62。

## 已取得的定向证据

| 层级 | 检查与实际结果 | 边界 |
|---|---|---|
| unit_verified | Pi 真实 SDK + 合成模型循环验证“发现规则阻断同批工具 → 下一轮收到规则 → 再次访问成功”，不调用真实模型/账号。 | 不证明真实 Provider 或项目审校速度。 |
| unit_verified | 原生 store 回归覆盖 MRU、项目导航竞态、预览恢复、收起状态、未读标记与 dirty settings 导航保护。 | 状态测试不替代真实操作系统交互。 |
| unit_verified（隔离 Electron 组件） | 现有 Electron 浏览器回归覆盖同名预览独立/复用、项目设置可见、改名徽标、后台终端收集/关闭、继续任务项目/岗位及失败不降级。 | 使用合成状态与 fake IPC，不读客户或日用数据。 |
| packaged_verified（定向界面） | 900px 宽窗口顶栏、从 Files 打开项目设置、右栏收起后的 Renderer 冷恢复及重新展开已通过。 | 同一打包探针也已验证侧栏项目名可见宽度；不代表所有窗口尺寸。 |
| 组件可见性对照 | “隐藏 A → 隐藏 B → 展开 B → 收起 B → 再展开 B”时，查看回调由上游的 `[1,2,2,2,2]` 改为 `[0,0,1,1,2]`。 | 证明该 React 生命周期，不推断所有协作通知已验证。 |

以上记录对应实施期间的真实执行；发布源码对应的远程检查另记如下，早期候选与正式产物的证据不混用。

## 最终本地检查

- `bun run test`：484 项通过，无失败（339 + 1 + 1 + 1 + 4 + 1 项 Bun，66 + 54 + 2 + 5 + 5 + 5 项 Node）。
- `bun run typecheck`：11 个包通过；`bun run check:boundaries` 与 `verify-host-seams` 通过，后者校验 12 个宿主接缝。
- 许可扫描、当前公开树隐私/署名扫描通过；新可达 25 个提交的作者禁用姓名扫描无命中。
- `smoke:vertical` 六步全部通过：打包、依赖、Agent、Chat、项目导航、Linguist；其中 Agent 19 项、Linguist 32 项通过。Native Open/Save 等人工项保留未覆盖。
- 构建时 HEAD 为 `b9417191`，工作树包含随后收口的基线/构建标识、测试位置与文档更新；这不是 clean-commit 发布资格。
- 此次未签名隔离验证包 `app.asar` SHA-256：`54a22ec739bcabbe22e1790003b5ae19ca6a1eda094c7ea6ce413053efd5ba36`。正式签名 Release 资产另行核验。

## 公开源码与发布结果

- 发布源码与 Tag 均为 `c85c7d8e206737adf92340001290f0bdf0a70620`；正式上游合并 `b9417191`、候选收口 `397a474f` 保留各自身份。
- [main CI 35739485354](https://github.com/wangyu-sg/linguist-agent-public/actions/runs/35739485354) attempt 1 成功。产物记录的源码与发布源码一致，`workingTreeDirty=false`，六步检查通过，Dock 检查 27 PASS。此记录补足前述本地 dirty 构建不能提供的发布源码对应证据。
- [Auto Release 35740252627](https://github.com/wangyu-sg/linguist-agent-public/actions/runs/35740252627) 成功；公开发布元数据确认非草稿、非预发布。
- 七项资产均为 uploaded：`latest-mac.yml`、`latest.yml`、macOS arm64/x64 各自的 DMG 和 ZIP，以及 Windows x64 EXE。资产列表存在及工作流成功不替代实际下载、哈希和签名核验。

## 正式包与日用安装核验

- macOS arm64/x64 ZIP 与 Windows EXE 的下载 SHA-512 均匹配最终更新清单，大小均匹配 GitHub 资产元数据；macOS YAML 同时提供并匹配 size，Windows YAML 未提供 size。
- 两个 macOS 包的 `codesign --verify --deep --strict` 及既有更新证书 requirement 均通过。签名检查在正常系统证书服务权限下执行；未降低签名或证书校验要求。
- 两个 macOS 包均包含正确的 `app-update.yml`；arm64 包通过真实 updater 配置读取。正式 arm64 `app.asar` SHA-256 为 `3b64b7b5f84e6295e1608c8198185cbc4f9649ab485364f5983a6f7de98b2da5`。
- 用户在发现旧安装缺失配置后，明确授权替换并重启。已备份旧应用并安装正式 arm64 包；安装后及启动后验签通过，ASAR 与下载包一致，关于页显示本次版本。
- 日用界面点击“检查更新”后，实际经历“正在检查”→“已是最新版本”，检查失败已恢复。没有清理或迁移项目/会话；没有运行 x64 或 Windows 产物。
- [发布机器证据](./evidence/0.18.1-release.json)记录正式资产、签名、配置、CI 与日用检查的范围。此次正式 arm64 包完成日用启动/更新检查，完整三模式自动检查由同提交的隔离 CI 包提供，不混写为正式签名包全量重测。

## 待补证据

- 用户在线更新端到端；人工 IME、Native Open/Save、VoiceOver、完整键盘操作及其他长期资格继续见 [已知限制](./KNOWN_LIMITATIONS.md)。

本轮没有重新使用客户批次评价翻译质量，也没有提供未经同条件测量的时间或 token 改善比例。0.18.0 发布与语言小样本结果保留在 [原记录](./VALIDATION_0_18_0.md)，不倒算为本版本的新证据。

## 日用旧安装的检查更新失败

本轮替换前只读核查发现，日用 `0.17.75` 本地修复包缺少 `Contents/Resources/app-update.yml`，且应用没有设置替代更新源。调用真实 `electron-updater.loadUpdateConfig()` 对该安装路径稳定复现 `ENOENT`，在网络请求和签名检查之前即失败。已有正式 `0.18.0` 两个 macOS 包均包含该配置且读取成功；公开更新端点、清单和对应资产可访问。

该证据证明当前安装存在本地配置阻断，不把它归为网络或签名错误。用户随后明确授权一次正式包替换，已保留旧应用备份并恢复检查更新。发布本身不会补回旧安装缺失的文件，因此这次采用已验签的正式包完成恢复。
