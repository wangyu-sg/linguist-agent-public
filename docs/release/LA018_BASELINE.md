# LA 0.18 实施起点

日期：2026-09-22。此文件为脱敏工程记录；不含客户文本、会话、账号或本机绝对路径。

- 本地起点 `276c2ea7508bc51ace5cdce4d1a8369a3cec3e08`，公开主分支 `85aa7e5da0f1ed63a6ed63d5bcd8a89c24fdd8a1`。保留已安装浏览器修复 `2468e3e2`；版本字符串相同不证明二进制相同。
- Proma 固定 `v0.19.53` / `f99edbdb594407ab190b97ae073889c5d96637ab`；Pi `0.85.1`；本轮没有升级依赖或共享 runtime。
- 默认 Phrase Skill 原为 `1.0.6`，已知私有项目覆盖高于默认版本，不能通过升级默认版本号整体抹去客户定制。
- 接口缺口：默认 apply 丢弃实际接受结果；接收建议路径本已提供 revision。上下文存在同页共有正文重复；规则已在顶层，不重复开发。
- 历史样本最近三个压缩窗口没有 context/apply 调用，故不能把其接口优化视为此前长压缩的唯一原因。
- 对指定窗口 16 次文档读取作工具 ID 定位与正文 hash 比较：9 次 metadata 续页与此前同文档正文完全相同，重复 71,796 Unicode 字符。仅支持本次 metadataOnly 的针对性修复，不用于推算 token、账单或全部延迟。
- 历史实际模型窗口、压缩精确起点、Provider 排队/重试不可完整还原，保留未知。脚本 `scripts/linguist-run-usage.ts` 只输出原始 usage 与计数，压缩前日志间隔仅为上界。

源代码/工具实际返回优先于方案文字。无客户任务被停、重跑或热切换。
