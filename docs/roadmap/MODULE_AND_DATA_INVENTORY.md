# Linguist Agent 模块与数据清单

审计日期：2026-07-22；LA-096 至 LA-101 增量更新：2026-07-23。路径和调用链以基线提交 `64bcb15b` 为准；`data/**` 未读取。每一行的“未知”都是实施前必须补证据的边界。

## 1. 模块清单

| 领域 | 当前文件与所有者 | 关键函数/调用链 | 数据路径 | 现有测试 | 未知项 |
|---|---|---|---|---|---|
| Task contract | `packages/cat-data/src/task_workspace_contract.ts` | DTO/event reducer contract；由 server routes、projections、desktop DTO 消费 | schema 内嵌于 snapshot/event | `task_workspace_contract.test.ts` | 旧 schema 在真实数据中的分布 |
| Task persistence | `packages/cat-data/src/task_workspace.ts` | `createTaskWorkspace` -> append event page -> write snapshot；open -> replay/repair | Project: `data/projects/<p>/task_workspace/tasks/<t>/{snapshot.json,events.jsonl}`；standalone: `data/assistant/tasks/<t>/...` | `task_workspace.test.ts`、`runtime_migrations.test.ts` | 跨进程 writer、fsync、磁盘满 |
| Message queue | Task workspace routes + message queue helper | renderer mutation -> authenticated route -> canonical queue -> Task event | Task 目录 `message_queue.json` | `task_message_queue.test.ts`、desktop conversation tests | crash window与幂等 key 覆盖率 |
| Live handles | `packages/cat-server/src/active_agent_runs.ts` | coordinator register/update/stop/remove | 仅进程内 | `active_agent_runs.test.ts` | runtime crash 后 handle 恢复语义 |
| Standalone General | `packages/cat-server/src/general_agent_runs.ts`、`cat-data/execution_profile.ts`、`cat-runtime/generalSessionPlan.ts` | route -> server-verified `ExecutionProfilePlan` -> immutable General Host/Worker plan -> Pi -> Task events；old direct model/effort is `custom`, default compatibility route is `balanced` | standalone Task、Pi session、resource manifest、hash-bound in-memory/serialized General profile plan | `execution_profile.test.ts`、`general_agent_runs.test.ts`、`general_session_plan.test.ts` | Fast/Best production configuration/UI、CAT/Team/Eval quality routing、真实 provider metadata与in-place rebind能力 |
| Project Main | `packages/cat-server/src/routes/task_workspace_routes.ts` | `handleTaskWorkspaceRoute` -> `runAgentStreaming` -> runtime session -> Task event | Project Task directory | `task_workspace_routes.test.ts`、`task_session_stop_bridge.test.ts` | 与 standalone coordinator 的重复边界 |
| Team | `workflow_routes.ts`、`workflow_plan.ts`、`workflow_artifacts.ts`、`workflow_eval_sqlite_cutover.ts` | preflight -> Team role -> child scope/artifact projection -> SQLite seam | legacy workflow JSON只作backup；SQLite workflow/artifact projections | `team_workflow_foundation.test.ts`、`sqlite_workflow_eval.test.ts` | 多状态词汇合并策略、真实并发/rollback |
| Eval | `eval_routes.ts`、`private_eval.ts`、`workflow_eval_sqlite_cutover.ts` | read-only Stable route -> canonical Eval Task/history -> SQLite run/output/scorecard/blind metadata | SQLite `workflow-eval.sqlite`；corpus/reference/rubric/reports仍为文件 | `private_eval*.test.ts`、`sqlite_workflow_eval.test.ts` | production UI迁出、真实历史/rollback |
| LA-025 aggregate recovery and closure gate | `packages/cat-server/src/cross_domain_sqlite_backup.ts`、`storage-sqlite` backup/restore、`tests/sqlite_legacy_writer_gate.test.ts` | fixed marker discovery -> SQLite/FK/blob-ref verification -> one aggregate manifest -> isolated staged restore；AST startup/import graph -> marker runtime legacy-write denial | seven authority markers/SQLite snapshots；Package/Library/CAT-core CAS roots；no server registration；旧reader/backup只读保留 | `sqlite_cross_domain_backup.test.ts`、`sqlite_legacy_writer_gate.test.ts`、per-domain storage recovery tests | 真实历史规模、跨进程 snapshot timing、power loss/disk full、production rollback；实际旧入口删除仍属LA-056 |
| Runtime adapter | `packages/cat-runtime/src/createGeneralAgentSession.ts`、`createCatAgentSession.ts`、`createMaintainerAgentSession.ts` | 各 factory 直接调用 Pi `createAgentSession` | Pi session directory | session tests、`pi_sessions.test.ts` | 未有 `AgentRuntimePort` characterization |
| Resource snapshot | `generalResourceSnapshot.ts`、`task_run_resources.ts`、`task_package_profile.ts` | discover -> hash/verify -> freeze manifest -> session load | Task `resource-profile.json`；manifest 在 canonical Run | `task_run_resources.test.ts`、extension tests | 尚非统一 ExecutionSnapshot |
| Permissions | `packages/cat-runtime/src/agentPermissions.ts`、server permission routes/decisions | settings -> normalize/build contract -> tool decision -> pending request -> response | Pi/settings JSON、Task permission events | `agent_permissions.test.ts`、`permission_decisions.test.ts`、desktop permissions tests | unknown mode fail-open；工具 metadata 完整性 |
| Standalone grants | `packages/cat-data/src/standalone_file_grants.ts` | create realpath/fingerprint -> persist -> Task scope；resolve -> revalidate | assistant file grant document + Task scope | standalone route/general run tests | 所有 path-shaped 参数是否被覆盖 |
| Structured settings/grants/trust | `packages/storage-sqlite/src/settings_grants_trust_repository.ts`、`packages/cat-server/src/settings_grants_trust_sqlite_cutover.ts`、adapted settings/grant/trust readers/writers | writer lease -> source inventory -> strict validation -> raw backup -> source/payload digest -> SQLite revision/CAS -> authority marker -> backend install | LA-owned settings/grants/trust projections；Provider secrets与Pi native settings显式排除 | `sqlite_settings_grants_trust.test.ts`、notification/Pi trust/Extension trust/team workflow regressions | 真实`data/**`/`~/.pi/agent/trust.json`、invalid legacy distribution、production startup/rollback |
| General guard | `packages/cat-runtime/src/generalRuntimeExtension.ts` | Pi `tool_call` hook -> collect paths -> grant/domain decision | 无独立 store | `general_agent_session.test.ts` | 参数名启发式绕过面 |
| Sandbox | `catSandbox.ts`、`generalSandbox.ts` | compute config -> `SandboxManager.updateConfig` -> wrap bash | env + module state | `cat_safety_kernel.test.ts`、permission tests | CAT/General 并发配置竞态 |
| Extension trust/host | `createGeneralAgentSession.ts`、`createCatAgentSession.ts`、`extensionHost.ts`、`extension_host_entry.ts`、`pi_extension_trust.ts` | snapshot -> canonical path/SHA approval -> content staging -> private exact-byte restaging -> isolated pure-tool RPC；Stable仍blocked | assistant trust JSON、snapshot manifest；Host临时目录不持久化 | `pi_trust.test.ts`、extension interaction、`extension_host.test.ts` | Stable产品激活Gate；Host v1不支持event/command/UI/render；真实签名包未验证 |
| Package Center / `.lapkg` format | `packages/cat-server/src/package_center.ts`（legacy只读实现待删除）、`lapkg_{format,signature,preview,activation,activation_journal,activation_recovery}.ts`、`lapkg_package_storage.ts`、`lapkg_sqlite_cutover.ts`、`legacy_package_inventory.ts`、routes、General resolver、Desktop Settings | npm catalog只读发现；旧npm写端点410；`.lapkg` strict verify/sign/Preview -> activation lock/stage/publish；LA-094后SQLite/WAL registry、activation journal/recovery projections与CAS content refs是v2唯一新writer；startup先完成Package authority/recovery，General只解析带`contentBlobRefId`的v2声明式资源；UI显示签名/风险并在无trust root时禁用 | legacy `data/assistant/capabilities/packages/{catalog-v1.json,installed-v1.json,.quarantine,installed}`只读且不解析执行；旧v2 `packages-v2/{registry-v2.json,content,.staging,activation-journal-v1.json,recovery-blocked-v1.json}`保留为read-only backup/派生验证对象；SQLite DB位于`data/runtime/package-registry-sqlite-v1/package-registry.sqlite`，Package CAS blob位于`data/assistant/capabilities/packages-v2/blob-store`，authority marker存在时旧writer fail closed；跨进程权威统一为runtime root的 `.data-root-writer-lease` | `package_center_routes.test.ts`、`lapkg_*test.ts`、`sqlite_lapkg_package_registry.test.ts`、`legacy_package_inventory.test.ts`、General Run与Desktop security tests；recovery suite覆盖全局lease与5个crash phase | 未读取真实legacy数据；legacy registry迁移只发布已验证resource bytes，若旧记录无archive bytes不臆造；production trust-root治理/存储仍待Decision；真实签名包、掉电和文件系统durability未真机验证 |
| Prompt / Execution Profile | `prompt_compiler.ts`、`execution_profile.ts`、Team/Eval routes、`server.ts::resolveModelPromptTokenBudget` | LA-032 explicit `ModelContextRegistry` + complete request budget -> `ready/needs_compaction/blocked`; LA-033 reuses that exact budget for standalone General Fast/Balanced/Best-or-custom immutable profile planning without a second registry; Team route serializes actual active `buildTeamEvidenceTools()` schemas before preparation | v2 manifest and profile plan record verified model/output plus request components; legacy prompt-only manifest and legacy no-profile snapshots remain read-only | `prompt_compiler.test.ts`、`execution_profile.test.ts`、General/Team/Eval route/context tests、`cat_prompt_isolation.test.ts` | provider tokenizer/framing and real billing evidence; General/CAT have no current PromptCompiler caller; Fast/Best standalone configuration and CAT/Team/Eval quality-profile rollout remain absent |
| Segment ContextGraph | `packages/cat-data/src/segment_context_graph.ts` | canonical owners/caller hydrate source ID + SHA-256 + revision -> pure frozen advisory profile/nodes/edges -> provenance-aware targeted retrieval; any missing/hash/revision mismatch invalidates the entire derived node/signal before ranking | no persistent graph, writer, source scan, filesystem, SQLite or runtime call; only caller-provided synthetic/verified provenance crosses the contract | `segment_context_graph.test.ts`、`segment_evidence.test.ts`、`project_context.test.ts`、`team_context_builder.test.ts` | no server evidence-hydration adapter, Tool/Run/UI integration or actual unified Memory/Library/Project Truth authority; synthetic graph observations do not establish CAT Evidence |
| TM-first Candidate Pipeline | `packages/cat-data/src/tm_candidate_pipeline.ts` | complete source/context/constraint/asset/model/profile/prompt/TM/repetition content address -> pure exact/repetition skip, high-fuzzy diff-repair, or full-generation route -> optional existing `SegmentProposalInput`; only existing proposal gate can persist | supplied non-authoritative process-memory cache only; no cache file/SQLite/Task/Batch write and no model invocation | `tm_candidate_pipeline.test.ts`、`tm_fuzzy_retrieval.test.ts`、`constraint_pack.test.ts`、`proposals.test.ts` | no canonical hydration adapter, semantic-continuity batching, model repair executor, persistent cache, server/router/Tool/Run/UI integration or real TM/asset revision evidence; `reuseSafety` cannot be self-certified |
| Independent Critic | `packages/cat-data/src/independent_critic.ts` | high-risk candidate identity + separate critic actor/execution/profile -> strict versioned advisory finding artifact -> exact finding/segment-only repair scope | no persistence, model/Team launch, Quality/Team ledger, proposal/target/Decision writer or UI integration | `independent_critic.test.ts`、`quality_audit.test.ts`、`team_quality_decision_ledger.test.ts`、`team_workflow_foundation.test.ts` | no live risk calibration/critic execution, finding merge/review priority, repair executor or real identity/provider isolation evidence |
| Batch consistency repair scope | `packages/cat-data/src/batch_consistency_repair.ts` | existing open QualityAudit consistency/terminology/voice finding -> exact findingId + segmentId + evidence-preserving `SegmentProposalInput`; locked row denied | no QA re-run, batch regeneration, proposal persistence, target write, model call or UI | `batch_consistency_repair.test.ts`、`quality_audit.test.ts`、`proposals.test.ts` | no automated repair, proposal persistence, cross-batch policy, real audit/runtime/UI integration |
| Memory | `assistant_memory.ts`、`assistant_memory_sqlite_cutover.ts`、`assistant_memory_repository.ts`、`tdai_memory_migration.ts`、memory tools/routes、General/CAT host bridge | Confirmed Memory 由 scoped SQLite projection/event 唯一写入。LA-029 支持 personal/client/franchise/project/locale、validity、explicit conflict key、user supersede/revoke；Host 以 lexical + managed local E5（pack 缺失则 lexical-only）选择冻结 snapshot，General/CAT Worker 不读 live Memory。LA-028 只把调用方显式只读 TDAI snapshot 变成 pending `MemoryCandidate` plan，只有 backup receipt + exact planHash + user confirmation 才进入该 writer | SQLite `data/runtime/assistant-memory-sqlite-v1/assistant-memory.sqlite` 为LA-095 marker后的唯一新writer；legacy personal/project `memories.json` 仅作raw backup/read-only rollback evidence；TDAI config/audit 是inventory-only，旧capture/store/recall无运行时入口；`tdai_embedding_bridge.ts`仍是独立显式 asset-vector compatibility adapter | `assistant_memory.test.ts`、`assistant_memory_evolution.test.ts`、`sqlite_assistant_memory.test.ts`、`assistant_library_routes.test.ts`、`runtime_hooks.test.ts`、`general_agent_session.test.ts`、`general_session_plan.test.ts`、desktop Library memory contract、TDAI/memory status/tools regressions | 真实memory分布/规模、实际 managed E5 cross-lingual quality/performance、可信 Client/Franchise mapping、已资格化 TDAI export adapter/asset inventory、批量确认UI、SQLite WAL/process-kill/power-loss/rollback |
| Library | `assistant_library.ts`、`assistant_library_repository.ts`、`assistant_library_sqlite_cutover.ts`、library routes/UI、CAT/General host bridge | startup lease -> strict legacy catalog/block/source parity -> SQLite metadata projection/event + CAS document blob -> injected read/write bridge -> lexical/vector retrieval | SQLite `data/runtime/assistant-library-sqlite-v1/assistant-library.sqlite` 为marker后的metadata唯一writer；CAS bytes在 `data/runtime/assistant-library-sqlite-v1/blob-store`；旧catalog/blocks与managed source只作raw backup/read-only provenance；`vectors.jsonl`仍为可重建非authority index | `sqlite_assistant_library.test.ts`、`assistant_library*.test.ts`、worker/route regressions | 真实Library分布/大文件streaming/blob GC、semantic pack/index rebuild cost、WAL/process-kill/power-loss/rollback |
| Project truth/CAT | `batch_workspace.ts`、`project_manifest.ts`、`tm.ts`、`termbase.ts`、asset typed/source helpers；proposal/quality/delivery/decision ledger由CAT governance域拥有 | LA-097 startup cutover -> CAT-core SQLite projection/event streams；LA-098 startup cutover -> CAT-governance SQLite streams/projections；Batch/Segment、Project manifest、TM、termbase/overrides、proposal/checklist/ledger/export audit各自只有一个SQLite writer；source/master/manifest asset bytes进入CAS refs；proposal evidence refs保留在projection，evidence bytes不在本票重复迁移 | legacy `data/projects/<p>/...` 保留为marker-linked backup/read-only evidence；CAT-core `data/runtime/cat-core-sqlite-v1/cat-core.sqlite`；CAT-governance `data/runtime/cat-governance-sqlite-v1/cat-governance.sqlite`；CAS `blob-store`；两域read-cache与asset indexes为派生排除项 | `sqlite_cat_core.test.ts`、`sqlite_cat_governance.test.ts`、batch/TM/termbase/asset/project-health/evidence/QA/delivery regressions；source digest、locked/tag/placeholder/revision、CAS、ledger hash/idempotency、proposal/checklist CAS、active-run block、old-writer denial、cross-process empty/read projection | 真实Project规模/历史格式、证据 bytes分布、跨进程 writer、WAL/blob GC、process-kill/power-loss/production rollback；CAT-governance实际跨Task/Team规模与全域删除仍未验证 |
| Document | `document_router_contract.ts`、`document_staging.ts`、`document_native_backend.ts`、`document_light_ocr_backend.ts`、`cat-runtime/{documentRouter,documentRouterBenchmarkPolicy}.ts`、`document_capabilities.ts`、installers、qualification、document tools | strict normalized block/result + Host private stage/digest/page inventory + host-staged parse/probe + page estimate -> native/light adapters -> server/worker-owned Router -> one HTTP/General-tool Artifact projection；only Router selects native/light/blocked per page；current versioned benchmark profile can change only its traced native threshold | `data/assistant/capabilities/documents/<id>`；private `data/assistant/document-staging` is `0700/0600`, source paths remain Host-only and disposal is immediate; startup plus each Router call removes stale `document-router-*` dirs after 24h; baseline policy is 64MiB/500pages/20k blocks/32MiB/5min/0.75 coverage. Profile v1 binds an expiry and evidence-report SHA-256, rejects unknown/version-invalid input and missing/expired profiles retain baseline. Optional MinerU needs exact managed runtime/package/model, hardware, metrics/crash/license evidence; Asset parse no longer accepts command/environment activation. Raw Router DTOs have no source path; the existing Task Artifact writer remains canonical | `document_router_contract.test.ts`、`document_staging.test.ts`、`document_native_backend.test.ts`、`document_light_ocr_backend.test.ts`、`document_router.test.ts`、`document_router_benchmark.test.ts`、`document_capabilities.test.ts`、`runtime_storage.test.ts`、document capability/route/tool/qualification tests | synthetic-only contracts. Real `pdfinfo`/startup cleanup/PDF complexity coverage/DOCX-XLSX locator、installed Paddle pack/quality/resource isolation、hardware/corpus benchmark、packaged UI未资格化；MinerU/other optional backends remain blocked |
| Storage ops | `data_root_writer_lease.ts`、`durable_file.ts`、`workspace.ts`、`runtime_storage.ts`、`runtime_migrations.ts` | startup取得唯一writer；critical file sync -> rename/append -> parent sync；normal cache保持temp/rename；classify/preview/cleanup；schema staged migration | runtime root lease、`data/`、cache/log roots、backup manifests | 双进程lease、fault points/ENOSPC、workspace/Task/decision/storage/migration/Package recovery | 实际SIGKILL/掉电；非关键audit/cache不具强耐久声明 |
| SQLite Task foundation/cutover/audit/blob/settings/Package/Memory/Library/CAT core/governance | `packages/storage-sqlite/src/{index,task_mapping_contract,legacy_task_importer,legacy_task_side_importer,task_workspace_repository,task_aggregate_backend,sqlite_audit_export,blob_store,settings_grants_trust_repository,assistant_memory_repository,assistant_library_repository,cat_core_repository,cat_governance_repository}.ts`；`cat-server/{task_aggregate_sqlite_cutover,task_aggregate_legacy_rollback,settings_grants_trust_sqlite_cutover,lapkg_package_storage,lapkg_sqlite_cutover,assistant_memory_sqlite_cutover,assistant_library_sqlite_cutover,cat_core_sqlite_cutover,cat_governance_sqlite_cutover,server_root}.ts`；`cat-data/{cat_core_storage,cat_governance_storage}.ts`；`scripts/export-sqlite-audit.ts` | Node `node:sqlite` schema-v2 WAL；单事务stream/events/projection/idempotency；revision与exact-projection CAS；authority-gated online backup/restore；hash-bound mapping；backup-first strict import；LA-089 Task startup marker/backend；LA-090只读deterministic hash-chained JSONL export/verify；LA-091 legacy authority marker、whole-domain rollback与fresh re-cutover；LA-092 SHA-256 immutable CAS blob、dedupe、revision-CAS ref manifest与orphan inspection/recovery foundation；LA-093 LA-owned settings/grants/trust strict import/parity/single cutover；LA-094 Package registry/journal/recovery single cutover与content refs；LA-095 Confirmed Memory strict import/parity/single cutover与host recall bridge；LA-096 Library metadata/blob strict import/parity/single cutover与CAT/General host bridge；LA-097 CAT-core manifest/batch/TM/termbase strict import/parity/single cutover与Batch/asset source refs；LA-098 CAT-governance proposal/checklist/ledger/export-audit strict import/parity/single cutover；LA-106显式test mode下server合成root/agent-dir覆盖 | Task、LA-093 structured domains、LA-094 Package、LA-095 Memory、LA-096 Library、LA-097 CAT core与LA-098 CAT governance均在writer lease下、listen前执行；synthetic tests only实际运行cutover/rollback/re-cutover；LA-092接受显式temporary root与authority；LA-093不迁Provider secrets或Pi native settings；LA-094旧Package file registry/journal/recovery marker后read-only，legacy materialized tree只作派生验证；LA-095旧memory JSON、LA-096旧Library metadata与LA-097旧CAT-core JSON marker后read-only，TDAI、semantic index、Library vectors、CAT read-cache/asset indexes显式排除；LA-098 evidence refs只随proposal projection保存，evidence bytes、vectors和read-cache显式排除；各域legacy writers在各自SQLite marker后read-only；Project quality ledger不再保持JSONL，audit CLI仍只读SQLite且无import writer；LA-106生产默认仍使用源码root，测试覆盖仅指向系统临时目录 | SQLite storage tests覆盖concurrency、rollback、mapping/import/repository、cutover/reload/partial-crash、backup completeness、active-run block、SQLite-only Task/pending recovery、old-writer guard、quality shadow refusal、authority loss、byte-identical export/redaction/hash chain/tamper/partial cleanup、whole-domain legacy rollback/fresh re-cutover，以及LA-092 CAS、LA-093 strict source validation/domain parity、LA-094 Package registry/blob/recovery/legacy-writer denial、LA-095 memory status/history/source/revision/scope/recall/no-auto-activation、LA-096 Library locator/block/count/hash/dedupe/orphan/lexical/reindex/old-writer cases、LA-097 CAT manifest/batch/TM/termbase/source-ref/CAS/old-writer/cross-process-read cases、LA-098 CAT governance ledger/proposal/checklist/audit/CAS/empty-cache/old-writer cases、LA-106 server root override/import-upload/asset-api synthetic integration | Node experimental API；Package/CAS/Memory/Library/CAT core/governance真实规模/流式写入、semantic index/TDAI迁移、blob GC、跨进程stale-lock、磁盘满/断电、真实`data/**`与`~/.pi/agent/trust.json`未扫描/迁移；真实规模/时长/WAL增长/process-kill/power-loss/生产rollback与大型audit内存占用未验证 |
| Local transport | `apps/desktop/src/runtime-{client,transport}.mjs`、`local_transport_{security,rendezvous}.ts`、server/LaunchAgent/installer | Keychain bootstrap -> verify signed `0600` rendezvous -> derive per-runtime credential -> random `0600` Unix socket HTTP/SSE；显式测试/一版升级才使用认证loopback | Keychain + user-level run metadata；Session credential不落盘 | rendezvous tamper/permission/port-squatter/reconnect、local security、resident/runtime compatibility、Desktop installer/package tests | 同UID恶意原生进程、XPC/audit token、真实安装升级仍未验证 |
| API request boundary | `strict_api_contract.ts`、`server.ts::readBody`、route body validators | request content-type/size/JSON-object -> shared declared top-level vocabulary -> endpoint/domain validation；permission、standalone/Project Task 与 queue 还拒绝 endpoint unknown field；field-use scan keeps the vocabulary complete | 无 writer；invalid input has stable `invalid_request`/`body_too_large`/`unsupported_media_type` envelope | `strict_api_contract.test.ts`、local transport、permission、Task/queue/batch/asset/Library/workflow route tests | response DTO generation and all endpoint-level value/query schemas remain incomplete |
| Electron boundary | `main.ts`、`preload.cts`、`ipc-contract.cts`、`workspace-capabilities.cjs`、`native-file-handles.mjs`、`desktop-security.mjs`、compiled `dist/electron/*` | trusted renderer -> fixed `api:workspace-capability` input -> preload/main capability+pathname revalidation -> main-owned method -> local runtime；pickers yield only ephemeral opaque native handles, and main rechecks canonical realpath/inode/type/TTL before injecting any existing server-native path; Project reveal uses canonical Project ID; typed stream/system IPC remain separate fixed channels；packaging copies only compiled Electron dependency allowlist plus built renderer | 无业务 store | desktop build/typecheck；native-handle forged/unknown/type/symlink/Project-root/cancel、`ipc-contract`、workspace-capability contract、security、packaging、local-update and full desktop tests | future normal endpoint and native-path entry must be registered/tested before renderer access; signed packaged app/real macOS, XSS/same-user process and long-lived handle expiry unproven |
| Renderer projection | `workspace-store.ts`、`task-events.ts::TaskProjectionStore` | selected Task: canonical snapshot -> `replace` -> subscribe cursor -> ordered `apply`; duplicate ignored, gap reloads without mutation, stale lower cursor snapshot refused; WorkspaceStore only publishes the projection into its existing facade | 每个选中 Task 一份内存 projection；snapshot+ordered events是唯一 Task 事实输入 | task-events/store/decision/permission/standalone-chat contract tests | store 仍承载非Task UI/请求副作用；后台 notification 未成为完整后台 projection，独立 Global Event Provider 仍未抽出 |
| Conversation UI | `TaskConversation.tsx`、`stream-event-coalescer.ts`、`ConversationItems.tsx`、composer/permission surfaces | store snapshot -> virtualized feed -> typed actions -> server mutation；仅 `assistant_delta` 以50ms合并，非delta先flush再立即按序交付，hidden flush、Task switch clear | canonical Task + ephemeral live reply；canonical events不节流 | coalescer/live-reply/conversation tests、UI contract | 真机长线程、后台节流、a11y、截图矩阵；全局 event provider 仍未抽出 |
| CAT UI | `renderer/cat/*` | Task/project scope -> segment data -> proposal/review/QA actions | 后端 Project truth | desktop CAT tests + backend route tests | 10k row真机计时与 VoiceOver |
| Pipeline UI | `PipelineWorkspace.tsx` | canonical Task view -> Quality/Delivery/Eval API -> artifacts | Task + eval/project data | pipeline model tests、eval tests | Eval 降级为工具后的保留面 |
| Settings/Package UI | `SettingsWorkspace.tsx`、settings data/model | parallel fetch -> sections -> typed mutations | server settings/package stores | desktop settings tests | `full` 移除、Maintainer/Package 导航归位 |
| Build/release | root/desktop manifests、`.github/workflows/*`、release scripts | npm ci -> root typecheck/test -> `mac:test`; release build/sign/notarize/publish | artifacts/zip/dmg/checksums | `mac_release.test.ts`、release candidate tests | release workflow action pins不一致；真签名未知 |

## 2. 关键数据路径

| 数据类别 | 当前 source of truth | 写入方式 | 恢复/一致性 | 迁移注意 |
|---|---|---|---|---|
| Canonical Task aggregate | SQLite Task/Run/event + 内嵌Decision + queue/resource profile；旧`snapshot.json`/`events.jsonl`/side files保留为cutover backup/rollback证据；Project quality ledger是独立Project/CAT governance域 | LA-089 startup marker后公共Task API只写SQLite；旧state-file factories fail closed；quality ledger由LA-098独立startup owner写入CAT-governance SQLite | event/projection事务、command idempotency、projection CAS、reopen；旧torn-final规则只用于import | synthetic Task/Decision/queue/profile parity、partial pre-marker recovery、marker/backup验证、post-cutover新Task与pending recovery已通过；真实历史未执行 |
| Task aggregate composition/cutover | `task_aggregate_storage.ts`、`task_workspace.ts`、`task_message_queue.ts`、`task_package_profile.ts`、`storage-sqlite/task_aggregate_backend.ts`、`task_aggregate_sqlite_cutover.ts`、`task_aggregate_legacy_rollback.ts` | startup只能为canonical root安装一次；LA-089持有唯一production factory import；SQLite marker后file state writers read-only；legacy marker下只允许显式旧authority；运行中不可替换 | runtime-schema migration -> lease-held inventory/backup/import/parity -> marker -> install -> canonical recovery -> listen；rollback为SQLite read-only export -> legacy files -> legacy marker；re-cutover为fresh DB import/parity -> SQLite marker | alternate-root/second install拒绝；active-run block；cutover/reload/partial crash；backup digest；quality exclusion；old-writer calls；SQLite-only Task/recovery；LA-090 deterministic audit export/verify；whole-domain rollback保留SQLite-only Task/queue；legacy startup不自动re-cutover；fresh re-cutover | 真实生产rollback、process-kill/power-loss和最大Task规模尚未完成 |
| Task queue/profile | Task 目录下 `message_queue.json`、`resource-profile.json` | JSON temp+rename + module queue | queue仅有updatedAt；profile有revision/hash，不能补造queue revision | LA-085已冻结stored order/profile canonical sort/approval digest；与Task事务边界仍分离 |
| Workflow | project workflow JSON/artifacts | 多处 `writeJsonFile` | route 自己协调 | 状态词汇需映射，不能直接文本替换 |
| Eval | `data/evals/private/<set>` 下 run/output/scorecard/review | JSON/JSONL 重写/追加 | 各模块校验 | canonical Eval Task 与 eval corpus 均须保留 |
| Project CAT | LA-097 marker后的SQLite CAT-core manifest/batch/TM/termbase projections + CAS source refs；LA-098 marker后的CAT-governance proposal/checklist/ledger/export-audit projections；proposal evidence refs保留在projection，evidence bytes不在本域重复迁移 | SQLite CAT-core repository是Batch/Segment/manifest/TM/termbase唯一writer；SQLite CAT-governance repository是proposal/checklist/ledger/export-audit唯一writer；旧文件只作backup/read-only | Batch source/master、manifest asset source digest与path ref；proposal evidence refs、ledger hash/idempotency、checklist/proposal CAS；两域read-cache/asset indexes可重建且不具authority | locked segment、tag/placeholder、revision/CAS、source digest、TM/termbase lookup、ledger parity、QA/waiver/delivery audit、active-run cutover block、old-writer denial、empty-project cross-process read projection | 真实数据分布/规模、证据 bytes分布、跨进程 writer、blob GC、process-kill/power-loss/production rollback；CAT governance全域删除与真实最大Project仍待后续 gate |
| Memory | LA-095 marker后的SQLite `assistant-memory.sqlite` projection/event streams；legacy personal/project `memories.json`仅raw backup/read-only | SQLite event/projection CAS；startup cutover在data-root lease下完成；LA-029 的 General/CAT Host 做 scoped lexical/local-semantic selection 并把不可变 snapshot 放入 attested plan，Worker 不读 live Memory；旧writer在marker后拒绝 | proposed/active/revoked/superseded、validity/conflict/source/revision/scope parity；scope-order semantic/lexical-only fallback；reopen、old-writer denial、no-auto-activation、recall-only/non-Evidence boundary | 真实data分布、managed E5 quality/performance、Client/Franchise mapping、TDAI迁移与生产rollback未验证 |
| Library | LA-096 marker后的SQLite metadata projection/event streams + CAS blob refs；legacy catalog/blocks/source只raw backup/read-only；vectors/index可重建非authority | SQLite projection CAS；managed bytes先hash入CAS，再保留可读source cache；CAT/General通过host persistence bridge读取写入 | provenance/locator/block/count/hash、dedupe、lexical parity、reindex、old-writer denial；semantic index不参与authority | 真实Library分布、blob GC、semantic quality、WAL/process-kill/power-loss/rollback未验证 |
| Package | SQLite Package registry/journal/recovery + CAS content refs；旧registry/tree只读 | startup lease -> synthetic legacy inventory/hash/resource import -> SQLite authority marker -> v2 activation/journal/recovery；新激活发布archive/resource CAS bytes | `planHash`、registry revision/CAS、contentBlobRefId、签名/树哈希与recovery journal | 未读取真实legacy Package；旧archive bytes可能缺失；真实包规模、签名包、掉电与production rollback仍未知 |
| Document capability | assistant capabilities documents | managed installer | qualification 状态 | 不得启用未资格化 MinerU fallback |
| Pi sessions/config | runtime/agent directories与 Keychain引用 | Pi/LA 混合所有权 | manifests、settings managers | secrets 不得迁入 repo/SQLite 明文 |
| Logs/audit | product log走`cat-data/safe_logging.ts` schema-v1 JSONL；server diagnostics同入口；其他domain audit仍各自拥有 | stdout/stderr；diagnostics append + 5 MiB/单archive rotation | sink前递归redaction；diagnostic ID；legacy read时净化但不重写原文件 | launchd旧日志真实规模/retention未真机验证；domain audit不是通用日志，后续迁移不得误删 |

## 3. 状态与写入所有权冲突

| 冲突 | 当前证据 | 目标约束 | 迁移前测试 |
|---|---|---|---|
| Run status 多词汇 | Task、workflow、eval、active registry、renderer 分别定义 | 单一 canonical transition owner | 每个旧状态到新状态的合法/非法映射 fixture |
| 资源冻结只覆盖部分 Run | General snapshot、Task resource manifest/profile hash | 全 Run immutable ExecutionSnapshot + explicit epoch | continuation 不扩张 resource set；config change 不隐式迁移 |
| 细粒度写入队列仍仅进程内 | Task/profile/grant promise queue；runtime已有dataRoot writer lease | revision/CAS与durability继续由LA-022及后续存储工单处理 | crash、lease外部破坏、revision conflict |
| API contract重复 | LA-038 已有 shared HTTP request schema/vocabulary；server DTO 与 `workspace-client.ts` 仍手写 | LA-040 已收敛普通 Electron transport 到固定 capability+pathname 模板；response/client parity仍需独立工单 | unknown field/content-type/boolean/array refusal；request/response schema parity仍未完成 |
| renderer path authority | `native-file-handles.mjs` + Project ID/opaque native handles；固定 workspace capability pathname | LA-040 拒绝任意 API pathname；LA-041 已将 native reveal/import/export 和 Project/Library/Package/Chat/document request path 改为 opaque handle或canonical ID，main是唯一解析者 | forged/unknown/type mismatch、symlink replacement、Project-root escape、cancel和raw-path surface denial |

## 4. 测试与命令现状

- 根：`npm run typecheck`；`npm test` 使用 `scripts/test-discovery.ts` 自动发现并串行运行 root tests。LA-117 为每个 child test 创建/清理独立 synthetic repo/Pi-agent root、覆盖继承root变量，并在执行前拒绝不继承环境的 direct literal `npm run server` launcher。LA-118 进一步让每个 child 的实际 `cwd` 成为无 `data/**` 的临时 root：仅以白名单暴露 `apps`、`contracts`、`docs`、`packages`、`patches`、`scripts`、`tests`、`node_modules` 及必要根配置，测试模块仍以 checkout 的绝对路径载入；`.git`、home 和未列 entry 均不可见。LA-119 只在 synthetic `.pi` 中补入复制的 `APPEND_SYSTEM.md`、`extensions/memory.ts` 和链接的 tracked `agents` profiles，仍保持 synthetic settings/skills/prompts，且不暴露 `.pi/npm`、其他 extensions、project settings 或 user trust。LA-120 只复制根 `AGENTS.md`，以保留 Dev preset 的 Pi project context；不复制 README、其他 docs 或任何 checkout ancestor。LA-121 只复制 `extensions/team-evidence-child.ts`，供 Team child production loader 的显式路径使用；它不随 settings 自动加载，也不开放其他 extensions。该source-level containment不是OS sandbox。
- 桌面：`npm --prefix apps/desktop run typecheck`、`npm --prefix apps/desktop test`；根 `npm run mac:test` 包装桌面验证。
- CI：`.github/workflows/ci.yml` 分别 `npm ci` 根与 desktop，并跑根 typecheck/test/mac:test；release workflows 使用未固定 SHA 的 `actions/*@v4`。
- 基线提交前已运行根/桌面 typecheck、根全量测试、桌面 149+3 测试和 `git diff --check`。托管 E5 pack 缺失导致其 acceptance 测试按合同跳过；未把该项记为 green。

## 5. 明确不存在或未找到的目标抽象

在排除 `node_modules` 的源码搜索中，统一 Runtime/Capability/Run/ExecutionSnapshot owner 已由前序 Tickets 落地；LA-023/LA-084/LA-085/LA-086 的 `storage-sqlite` 仍是无production caller的 synthetic foundation。LA-086 importer只接受显式synthetic source/backup路径和writer authority，不是当前 LA 事实库、startup migration或production backup。
